<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.OrderMapper">

    <!-- 전체 목록 (기존 유지) -->
    <select id="selectOrderList" resultType="Order">
        SELECT
            id,
            order_no        AS orderNo,
            total_amount    AS totalAmount,
            discount_amount AS discountAmount,
            final_amount    AS finalAmount,
            payment_type    AS paymentType,
            status,
            note,
            ordered_at      AS orderedAt
        FROM orders
        ORDER BY ordered_at DESC
    </select>

    <!-- 페이징 목록 -->
    <select id="selectOrderByPage" parameterType="PageRequest" resultType="Order">
        SELECT
            id,
            order_no        AS orderNo,
            total_amount    AS totalAmount,
            discount_amount AS discountAmount,
            final_amount    AS finalAmount,
            payment_type    AS paymentType,
            status,
            note,
            ordered_at      AS orderedAt
        FROM orders
        <where>
            <if test="keyword != null and keyword != ''">
                AND (order_no LIKE CONCAT('%', #{keyword}, '%')
                  OR payment_type LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND status = #{category}
            </if>
            <if test="dateFrom != null and dateFrom != ''">
                AND DATE(ordered_at) &gt;= #{dateFrom}
            </if>
            <if test="dateTo != null and dateTo != ''">
                AND DATE(ordered_at) &lt;= #{dateTo}
            </if>
        </where>
        ORDER BY ordered_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 페이징 카운트 -->
    <select id="countOrder" parameterType="PageRequest" resultType="int">
        SELECT COUNT(*) FROM orders
        <where>
            <if test="keyword != null and keyword != ''">
                AND (order_no LIKE CONCAT('%', #{keyword}, '%')
                  OR payment_type LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND status = #{category}
            </if>
            <if test="dateFrom != null and dateFrom != ''">
                AND DATE(ordered_at) &gt;= #{dateFrom}
            </if>
            <if test="dateTo != null and dateTo != ''">
                AND DATE(ordered_at) &lt;= #{dateTo}
            </if>
        </where>
    </select>

    <insert id="insertOrder" parameterType="Order"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (
            order_no, total_amount, discount_amount,
            final_amount, payment_type, status, note
        ) VALUES (
            #{orderNo}, #{totalAmount}, #{discountAmount},
            #{finalAmount}, #{paymentType}, #{status}, #{note}
        )
    </insert>

    <insert id="insertOrderItem" parameterType="OrderItem">
        INSERT INTO order_items (
            order_id, menu_id, qty, unit_price, subtotal
        ) VALUES (
            #{orderId}, #{menuId}, #{qty}, #{unitPrice}, #{subtotal}
        )
    </insert>

    <select id="selectMenuList" resultType="Menu">
        SELECT
            id,
            category_id AS categoryId,
            name,
            description,
            price,
            cost,
            is_available AS isAvailable
        FROM menus
        WHERE is_available = 1
        ORDER BY category_id, name
    </select>

    <update id="updateOrderStatus" parameterType="Order">
        UPDATE orders SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteOrder" parameterType="Long">
        DELETE FROM orders WHERE id = #{id}
    </delete>

</mapper>